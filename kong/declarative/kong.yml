_format_version: "3.0"

services:
  - name: example-service
    url: http://httpbin.org
    routes:
      - name: plugin-required
        paths:
          - /admin
          - /private
        plugins:
          - name: jwt-introspect
            config:
              key: "uiiuuiuiu"
      - name: anything
        paths:
          - /anything
